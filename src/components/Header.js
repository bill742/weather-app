const Header = () => {
    const template = `
        <header>
            <h1>Weather App</h1>
            <h2>See your weather conditions or search for the forecast in cities around the world
            </h2>
        </header>
    `;

    return template;
};

export default Header;
